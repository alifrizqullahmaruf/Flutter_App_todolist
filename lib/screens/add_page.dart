import 'package:flutter/material.dart';

class AddPage extends StatelessWidget {
  const AddPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add Navigation'),
        automaticallyImplyLeading: false,
      ),
      body: SafeArea(
          child: Column(
        children: [],
      )),
    );
  }
}
